export function generatePhotoUploader({ src }) {
  let image = src || "default.png";
  let markup = `
    <form class="upload-profile-picture" method="POST" urlencoded="" action="/">
    </form>
  `;
}
